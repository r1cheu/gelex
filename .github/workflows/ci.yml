name: CI

on:
  push:
    branches: [main]
    paths-ignore:
      - "*.md"
      - "docs/**"
      - "LICENSE"
  pull_request:
    branches: [main]
    paths-ignore:
      - "*.md"
      - "docs/**"
      - "LICENSE"

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - uses: pre-commit/action@v3.0.1

  build-test:
    name: Build & Test
    runs-on: ubuntu-latest
    env:
      SCCACHE_GHA_ENABLED: "true"
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Setup pixi
        uses: prefix-dev/setup-pixi@v0.9.2
        with:
          cache: true
          cache-write: ${{ github.event_name == 'push' && github.ref_name == 'main' }}

      - name: setup sccache-cache
        uses: mozilla-actions/sccache-action@v0.0.9

      - name: Build & Run tests
        run: pixi run test

  # coverage:
  #   name: Coverage
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v4
  #       with:
  #         submodules: recursive
  #
  #     - name: Setup pixi
  #       uses: prefix-dev/setup-pixi@v0.9.2
  #       with:
  #         cache: true
  #         cache-write: false
  #
  #     - name: Generate coverage report
  #       run: pixi run coverage
  #
  #     - name: Upload coverage report
  #       uses: actions/upload-artifact@v4
  #       with:
  #         name: coverage-report
  #         path: build/coverage/
  #         retention-days: 30

  nightly:
    name: Nightly Release
    runs-on: ubuntu-latest
    env:
      SCCACHE_GHA_ENABLED: "true"
    needs: build-test
    if: github.event_name == 'push' && github.ref_name == 'main'
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Setup pixi
        uses: prefix-dev/setup-pixi@v0.9.2
        with:
          cache: true
          cache-write: false

      - name: Setup sccache
        uses: mozilla-actions/sccache-action@v0.0.9

      - name: Build conda package
        run: |
          pixi run rattler-build build \
            --no-build-id \
            --recipe recipe.yaml \
            --output-dir dist/

      - name: Check if version exists on prefix.dev
        id: check_version
        run: |
          VERSION=$(grep -oP 'version:\s*"\K[^"]+' recipe.yaml | head -1)
          echo "Package version: $VERSION"
          if pixi search -c https://prefix.dev/gelex gelex 2>/dev/null | grep -q "$VERSION"; then
            echo "Version $VERSION already exists, will use --force"
            echo "upload_flags=--force" >> "$GITHUB_OUTPUT"
          else
            echo "Version $VERSION is new"
            echo "upload_flags=" >> "$GITHUB_OUTPUT"
          fi

      - name: Upload to prefix.dev
        run: |
          pixi run rattler-build upload prefix \
            --channel gelex \
            --api-key ${{ secrets.PREFIX_API_KEY }} \
            ${{ steps.check_version.outputs.upload_flags }} \
            dist/**/*.conda
